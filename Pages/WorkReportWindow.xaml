<Window x:Class="PEOcleanWPFApp.Pages.WorkReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PEOcleanWPFApp.Pages"
        mc:Ignorable="d"
        Title="Запись о работе за [дата]" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowState="Normal" ResizeMode="CanResize">
    <Window.Resources>
        <LinearGradientBrush x:Key="PrimaryBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#ffffff" Offset="0"/>
            <GradientStop Color="#f8f9fa" Offset="0.3"/>
            <GradientStop Color="#e9ecef" Offset="0.7"/>
            <GradientStop Color="#dee2e6" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1976d2"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#424242"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4caf50"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#f44336"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#bdbdbd"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#ff9800"/>

        <DropShadowEffect x:Key="PlasticShadow" Color="#000000" Opacity="0.1" BlurRadius="8" ShadowDepth="3"/>
        <DropShadowEffect x:Key="ButtonGlow" Color="#1976d2" Opacity="0.3" BlurRadius="15" ShadowDepth="0"/>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="2" Effect="{StaticResource PlasticShadow}">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#ffffff" Offset="0"/>
                <GradientStop Color="#fafafa" Offset="0.3"/>
                <GradientStop Color="#f5f5f5" Offset="0.7"/>
                <GradientStop Color="#eeeeee" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>
            <ResizeGrip HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1"/>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" MouseLeftButtonDown="Window_MouseLeftButtonDown" Effect="{StaticResource PlasticShadow}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="{Binding WindowTitle}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="16" FontWeight="Bold" Effect="{StaticResource PlasticShadow}"/>
                        <TextBlock Text="{Binding EmployeeInfo}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Content="🗗" Margin="5,0" Click="MinimizeButton_Click" Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}" BorderThickness="0" FontSize="16" Width="30"/>
                        <Button Content="⬜" Margin="5,0" Click="MaximizeButton_Click" Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}" BorderThickness="0" FontSize="16" Width="30"/>
                        <Button Content="✕" Margin="5,0" Click="CloseButton_Click" Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}" BorderThickness="0" FontSize="16" Width="30"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Основная область -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15">
                <StackPanel>
                    <!-- Выбор домов -->
                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,15" Effect="{StaticResource PlasticShadow}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#ffffff" Offset="0"/>
                                <GradientStop Color="#f8f9fa" Offset="0.5"/>
                                <GradientStop Color="#e9ecef" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel Margin="15">
                            <TextBlock Text="Выберите дома, на которых работал сотрудник" FontWeight="Bold" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryBrush}"/>

                            <ItemsControl x:Name="HousesItemsControl" ItemsSource="{Binding AvailableHouses}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,5" Foreground="{Binding TextColor}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Button Content="+ Добавить временный дом" Margin="0,10,0,0" Click="AddTemporaryHouseButton_Click" Background="{StaticResource SuccessBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0" Effect="{StaticResource ButtonGlow}"/>
                        </StackPanel>
                    </Border>

                    <!-- Блоки по домам -->
                    <ItemsControl x:Name="HouseWorkItemsControl" ItemsSource="{Binding SelectedHouses}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,15" Effect="{StaticResource PlasticShadow}">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#ffffff" Offset="0"/>
                                            <GradientStop Color="#f8f9fa" Offset="0.5"/>
                                            <GradientStop Color="#e9ecef" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <StackPanel Margin="15">
                                        <TextBlock Text="{Binding HouseName}" FontWeight="Bold" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryBrush}"/>

                                        <!-- График работ -->
                                        <TextBlock Text="График работ на этот день:" FontWeight="Bold" Margin="0,0,0,5"/>
                                        <ItemsControl ItemsSource="{Binding ScheduledWorks}" Margin="0,0,0,10">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- Фактически выполнено -->
                                        <TextBlock Text="Фактически выполнено:" FontWeight="Bold" Margin="0,0,0,5"/>
                                        <ItemsControl ItemsSource="{Binding ActualWorks}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <CheckBox Content="{Binding WorkName}" IsChecked="{Binding IsCompleted, Mode=TwoWay}" Margin="0,2"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- Комментарий к дому -->
                                        <TextBlock Text="Комментарий к дому:" Margin="0,10,0,5"/>
                                        <TextBox Text="{Binding HouseComment, Mode=TwoWay}" Height="40" TextWrapping="Wrap" AcceptsReturn="True"/>

                                        <!-- Фотоотчет -->
                                        <TextBlock Text="Фотоотчёт:" FontWeight="Bold" Margin="0,10,0,5"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                            <Button Content="Прикрепить фото" Click="AttachPhotoButton_Click" CommandParameter="{Binding}" Background="{StaticResource SuccessBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0" Effect="{StaticResource ButtonGlow}"/>
                                            <TextBlock Text="{Binding PhotoStatus}" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{Binding PhotoStatusColor}"/>
                                        </StackPanel>
                                        <ItemsControl ItemsSource="{Binding PhotoFiles}" Margin="0,0,0,10">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,2">
                                                        <TextBlock Text="📷" Margin="0,0,5,0"/>
                                                        <TextBlock Text="{Binding FileName}" VerticalAlignment="Center"/>
                                                        <Button Content="❌" Margin="10,0,0,0" Click="RemovePhotoButton_Click" CommandParameter="{Binding}" Background="Transparent" Foreground="{StaticResource ErrorBrush}" BorderThickness="0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Общие поля -->
                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,15" Effect="{StaticResource PlasticShadow}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#ffffff" Offset="0"/>
                                <GradientStop Color="#f8f9fa" Offset="0.5"/>
                                <GradientStop Color="#e9ecef" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel Margin="15">
                            <TextBlock Text="Общий комментарий:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox x:Name="GeneralCommentTextBox" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15"/>

                            <TextBlock Text="Статус подтверждения:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <StackPanel>
                                <RadioButton x:Name="ConfirmedRadioButton" Content="Работа подтверждена — начислить оплату" IsChecked="True" Margin="0,5" GroupName="ConfirmationStatus"/>
                                <RadioButton x:Name="NotConfirmedRadioButton" Content="Не подтверждена — не оплачивать" Margin="0,5" GroupName="ConfirmationStatus"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                    <RadioButton x:Name="PartialRadioButton" Content="Частичное выполнение — оплатить" VerticalAlignment="Center" GroupName="ConfirmationStatus"/>
                                    <TextBox x:Name="PartialPercentageTextBox" Width="50" Margin="10,0" Text="50" IsEnabled="{Binding IsChecked, ElementName=PartialRadioButton}"/>
                                    <TextBlock Text="%" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Кнопки управления -->
            <Border Grid.Row="2" Background="{StaticResource PrimaryBrush}" Effect="{StaticResource PlasticShadow}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
                    <Button Content="Сохранить и закрыть" Width="150" Margin="0,0,10,0" Click="SaveAndCloseButton_Click" Background="{StaticResource SuccessBrush}" Foreground="White" BorderThickness="0" Effect="{StaticResource ButtonGlow}"/>
                    <Button Content="Сохранить и продолжить" Width="160" Margin="0,0,10,0" Click="SaveAndContinueButton_Click" Background="{StaticResource SuccessBrush}" Foreground="White" BorderThickness="0" Effect="{StaticResource ButtonGlow}"/>
                    <Button Content="Отметить как неявку" Width="140" Margin="0,0,10,0" Click="MarkAbsentButton_Click" Background="{StaticResource ErrorBrush}" Foreground="White" BorderThickness="0" Effect="{StaticResource ButtonGlow}"/>
                    <Button Content="Отмена" Width="80" Click="CancelButton_Click" Background="{StaticResource ErrorBrush}" Foreground="White" BorderThickness="0" Effect="{StaticResource ButtonGlow}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
