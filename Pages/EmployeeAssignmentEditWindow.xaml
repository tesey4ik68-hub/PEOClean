<Window x:Class="PEOcleanWPFApp.Pages.EmployeeAssignmentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PEOcleanWPFApp.Pages"
        mc:Ignorable="d"
        Title="Назначить сотрудника на дом" Height="500" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Назначить сотрудника на дом" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Выбор сотрудника -->
        <TextBlock Grid.Row="1" Text="Сотрудник:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="2" x:Name="EmployeeComboBox" Margin="0,0,0,15" DisplayMemberPath="FullName"/>

        <!-- Выбор дома -->
        <TextBlock Grid.Row="3" Text="Дом:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="4" x:Name="HouseComboBox" Margin="0,0,0,15" DisplayMemberPath="Address"/>

        <!-- Выбор роли -->
        <TextBlock Grid.Row="5" Text="Роль:" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,0,0,15">
            <CheckBox x:Name="JanitorCheckBox" Content="Дворник" Margin="0,0,20,0"/>
            <CheckBox x:Name="CleanerCheckBox" Content="Уборщик"/>
        </StackPanel>

        <!-- Период назначения -->
        <GroupBox Grid.Row="7" Header="Период назначения" Margin="0,0,0,15">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Начало:" Width="100" VerticalAlignment="Center"/>
                    <DatePicker x:Name="StartDatePicker" Width="150"/>
                </StackPanel>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Окончание:" Width="100" VerticalAlignment="Center"/>
                    <DatePicker x:Name="EndDatePicker" Width="150"/>
                </StackPanel>
                
                <CheckBox Grid.Row="2" x:Name="IsPrimaryCheckBox" Content="Основной"/>
            </Grid>
        </GroupBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Назначить" Width="100" Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="100" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>