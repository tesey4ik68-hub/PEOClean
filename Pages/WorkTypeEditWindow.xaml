<Window x:Class="PEOcleanWPFApp.Pages.WorkTypeEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PEOcleanWPFApp.Pages"
        mc:Ignorable="d"
        Title="Редактирование вида работ" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowState="Normal" ResizeMode="CanResize">
    <Window.Resources>
        <LinearGradientBrush x:Key="PrimaryBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#ffffff" Offset="0"/>
            <GradientStop Color="#f8f9fa" Offset="0.3"/>
            <GradientStop Color="#e9ecef" Offset="0.7"/>
            <GradientStop Color="#dee2e6" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1976d2"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#424242"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4caf50"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#f44336"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#bdbdbd"/>

        <DropShadowEffect x:Key="PlasticShadow" Color="#000000" Opacity="0.1" BlurRadius="8" ShadowDepth="3"/>
        <DropShadowEffect x:Key="ButtonGlow" Color="#1976d2" Opacity="0.3" BlurRadius="15" ShadowDepth="0"/>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="2" Effect="{StaticResource PlasticShadow}">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#ffffff" Offset="0"/>
                <GradientStop Color="#fafafa" Offset="0.3"/>
                <GradientStop Color="#f5f5f5" Offset="0.7"/>
                <GradientStop Color="#eeeeee" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>
            <ResizeGrip HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1"/>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" MouseLeftButtonDown="Window_MouseLeftButtonDown" Effect="{StaticResource PlasticShadow}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Редактирование вида работ"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="18"
                               FontWeight="Bold"
                               VerticalAlignment="Center"
                               Margin="20,0,0,0"
                               Effect="{StaticResource PlasticShadow}"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Content="🗗" Margin="5,0" Click="MinimizeButton_Click" Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}" BorderThickness="0" FontSize="16" Width="30"/>
                        <Button Content="⬜" Margin="5,0" Click="MaximizeButton_Click" Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}" BorderThickness="0" FontSize="16" Width="30"/>
                        <Button Content="✕" Margin="5,0" Click="CloseButton_Click" Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}" BorderThickness="0" FontSize="16" Width="30"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Форма редактирования -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20">
                <StackPanel>
                    <TextBlock Text="Название" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBox x:Name="NameTextBox" Margin="0,0,0,15" Padding="5"/>

                    <TextBlock Text="Код" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBox x:Name="CodeTextBox" Margin="0,0,0,15" Padding="5" MaxLength="10"/>

                    <TextBlock Text="Единица измерения" FontWeight="Bold" Margin="0,10,0,5"/>
                    <ComboBox x:Name="UnitOfMeasureComboBox" Margin="0,0,0,15" Padding="5">
                        <ComboBoxItem Content="Дом"/>
                        <ComboBoxItem Content="Подъезд"/>
                        <ComboBoxItem Content="м²"/>
                        <ComboBoxItem Content="шт."/>
                        <ComboBoxItem Content="час"/>
                        <ComboBoxItem Content="раз"/>
                    </ComboBox>

                    <TextBlock Text="Описание" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBox x:Name="DescriptionTextBox" Margin="0,0,0,15" Padding="5" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <CheckBox x:Name="RequiresPhotoCheckBox" Content="Требует фотоотчёт" Margin="0,10,0,15"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Кнопки действий -->
            <Border Grid.Row="2" Background="{StaticResource PrimaryBrush}" Effect="{StaticResource PlasticShadow}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
                    <Button Content="Сохранить" Width="100" Margin="0,0,10,0" Click="SaveButton_Click" Background="{StaticResource SuccessBrush}" Foreground="White" BorderThickness="0" Effect="{StaticResource ButtonGlow}"/>
                    <Button Content="Отмена" Width="100" Click="CancelButton_Click" Background="{StaticResource ErrorBrush}" Foreground="White" BorderThickness="0" Effect="{StaticResource ButtonGlow}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
